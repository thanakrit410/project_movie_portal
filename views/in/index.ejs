<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/st/index.css">
    <title>test</title>
</head>
<body>
    <div class="container">
        <header class="blog-header py-3">
          <div class="row flex-nowrap justify-content-between align-items-center">
            <div class="col-4 pt-1">
            </div>
            <div class="col-4 logo">
                <img src="/im/RPC-JP_Logo.png" alt="logo" width="60" height="60"/>
            </div>
           <% if(!currentUser){ %>
            <div class="col-4 d-flex justify-content-end align-items-center">
              <a class="link-secondary" href="#" aria-label="Search">
                <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" class="mx-3" role="img" viewBox="0 0 24 24"><title>Search</title><circle cx="10.5" cy="10.5" r="7.5"></circle><path d="M21 21l-5.2-5.2"></path></svg>
              </a>
              <a class="btn btn-sm btn-outline-secondary" href="/login">Login</a>
            </div>
            <% }else{ %>
              <div class="col-4 d-flex justify-content-end align-items-center">
                <a class="link-secondary" href="#">
                   Logged as <%= currentUser.username %>
                </a>
                <a class="btn btn-sm btn-outline-secondary" href="/logout">Log out</a>
              </div>
            <% } %>
          </div>
        </header>

      
        <div class="nav-scroller py-1 mb-2">
          <nav class="nav d-flex justify-content-between">
            <button class="p-2 link-secondary" onclick = "document.location = '/showmovie'">HOME</button>
            <button class="p-2 link-secondary" onclick = "document.location = '/showmovie/movie'">MOVIE</button>
            <button class="p-2 link-secondary" onclick = "document.location = '/Theater'">CINEMA</button>
            <button class="p-2 link-secondary" href="#">PROMOTION</button>
            <% if(!currentUser){ %>
            <button class="p-2 link-secondary" onclick = "document.location = '/login'">ME</button>
            <% }else{ %>
             
              
              <button class="p-2 link-secondary" onclick = "document.location = '/user/<%= currentUser._id %>'">ME</button>

            <% } %>
          </nav>
        </div>
    </div>
    <% if(error && error.length>0){ %>
      <div class= 'alert alert-danger' role = 'alert'>
        <%=  error %>
      </div>
    <% } %>
    <% if(success && success.length>0){ %>
      <div class= 'alert alert-success' role = 'alert'>
        <%=  success %>
      </div>
    <% } %>
    <div class ="slide">
        <figure>
          <img src="/im/poster1.jpg">
          <img src="/im/poster2.jpg">
          <img src="/im/poster3.jpg">
          <img src="/im/poster1.jpg">
          <img src="/im/poster2.jpg">
        </figure>
    </div>
    
    <div class="searchbar container w-50 mt-5">
      <form class="d-flex" action="/search/keyword" method="POST">
        <input class="form-control me-2" type="search" name="keyword" placeholder="Search for item" aria-label="Search">
        <button class="btn btn-outline-primary" type="submit">Search</button>
      </form>
    </div>

    <form action="/sort/aToz" method="GET">
    <select id="selete" name="">
      <option value="#">Sort By</option>
      <option value="/search/aToz">Price : Low to High</option>
    </select>
    <button class="p-2 link-secondary" type="submit">Sort</button>
    </form>
     